@include('partials.navegacion')
<main class="zona-trabajo"><div class="encabezado-seccion"><h2>Crear cuenta de residente</h2></div><section class="contenedor-formulario"><p><strong>{{ $residente->nombre }}</strong><br>{{ $residente->apartamentos->map(fn ($apartamento) => $apartamento->torre.' - Apto. '.$apartamento->numero)->join(', ') }}</p><form class="formulario-paquete" method="post" action="{{ route('residentes.cuenta.store',$residente) }}">@csrf<input name="email" type="email" value="{{ old('email',$residente->correo) }}" placeholder="Correo de acceso" required><input name="password" type="password" placeholder="Contraseña" required><input name="password_confirmation" type="password" placeholder="Confirmar contraseña" required><button class="btn-notificar">Crear cuenta</button></form></section></main>
