@include('partials.navegacion')
<main class="zona-trabajo"><div class="contenedor-formulario"><form class="formulario-paquete" method="post" action="{{ route('residentes.update',$residente) }}">@csrf @method('PATCH')<input name="nombre" value="{{ old('nombre',$residente->nombre) }}" required><input name="telefono" value="{{ old('telefono',$residente->telefono) }}"><input name="correo" type="email" value="{{ old('correo',$residente->correo) }}">@foreach($apartamentos as $apartamento)<label><input type="checkbox" name="apartamentos[]" value="{{ $apartamento->id }}" @checked($residente->apartamentos->contains($apartamento))>{{ $apartamento->torre }} - {{ $apartamento->numero }}</label>@endforeach @if($residente->user)<input name="email_acceso" type="email" value="{{ old('email_acceso',$residente->user->email) }}"><input name="password" type="password" placeholder="Nueva contraseña opcional"><input name="password_confirmation" type="password" placeholder="Confirmar contraseña">@endif<button class="btn-notificar">Actualizar residente</button></form></div></main>
